---
"@tinloof/sanity-extends": minor
---

Fix field merging to correctly handle duplicate field names: child fields now override all parent fields with the same name while preserving duplicates within the same schema
